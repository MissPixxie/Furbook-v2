// import { User } from "@/components/types";
// import React from "react";

// export async function GET(request: Request, { id }): Promise<Response> {
//   //const userId = request.expoUrl.searchParams.get("user");
//   // Fetch data from external API

//   const response = await fetch("http://localhost:8001/dogs/" + id);
//   // Check for successful response
//   if (!response.ok) {
//     return new Response(null, { status: response.status });
//   } // Parse the response as JSON
//   const data = await response.json(); // Return the data as a JSON response
//   return Response.json(data);
// }
